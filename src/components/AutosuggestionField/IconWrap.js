import React from "react";

const IconWrap = ({ iconClass }) => {
    return (
        <div className="button-icon">
            <i className={iconClass} />
        </div>
    )
};

export default IconWrap;